{% if page.tags %}
    <ul class="inline">
        <li class="legend">Publié dans :</li>
        {% for tag in page.tags %}
            <li>{{ tag }}</li>
        {%  endfor %}
    </ul>
{% endif %}
